{
  "name": "eleventy-starter",
  "version": "0.0.1",
  "description": "A starting point for Eleventy projects with Tailwind and other goodies.",
  "author": "Matt Waler",
  "repository": {
    "type": "git",
    "url": "https://github.com/mattwaler/eleventy-starter"
  },
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "yarn clean && NODE_ENV=production concurrently 'yarn:build:*' --raw",
    "build:css": "postcss src/_styles/main.pcss -o dist/main.css",
    "build:js": "parcel build src/_scripts/main.js --no-source-maps",
    "build:markup": "eleventy --config 11ty.config.js",
    "clean": "rm -rf .cache dist",
    "dev": "yarn clean && concurrently 'yarn:dev:*' --raw",
    "dev:css": "watch 'yarn css' src/_styles",
    "dev:js": "parcel watch src/_scripts/main.js",
    "dev:markup": "eleventy --serve --quiet --config 11ty.config.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.9.0",
    "@fullhuman/postcss-purgecss": "^1.3.0",
    "@starptech/prettyhtml": "^0.10.0",
    "autoprefixer": "^9.6.1",
    "axios": "^0.19.0",
    "concurrently": "^4.1.2",
    "cssnano": "^4.1.10",
    "dotenv": "^8.1.0",
    "eslint": "^6.2.2",
    "eslint-plugin-import": "^2.18.2",
    "eslint-plugin-node": "^10.0.0",
    "eslint-plugin-promise": "^4.2.1",
    "parcel-bundler": "^1.12.3",
    "postcss-cli": "^6.1.3",
    "postcss-import": "^12.0.1",
    "tailwindcss": "^1.1.2",
    "watch": "^1.0.2"
  }
}
