{
  "name": "eleventy-starter",
  "version": "0.0.1",
  "description": "A starting point for Eleventy projects with Tailwind and other goodies.",
  "author": "Matt Waler",
  "repository": {
    "type": "git",
    "url": "https://github.com/mattwaler/eleventy-starter"
  },
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "yarn clean && concurrently 'yarn:build:*' --raw",
    "build:css": "parcel build src/_styles/main.pcss --no-source-maps",
    "build:js": "parcel build src/_scripts/main.js --no-source-maps",
    "build:markup": "eleventy --config 11ty.config.js",
    "clean": "rm -rf .cache dist",
    "dev": "yarn clean && concurrently 'yarn:dev:*' --raw",
    "dev:css": "parcel watch src/_styles/main.pcss",
    "dev:js": "parcel watch src/_scripts/main.js",
    "dev:markup": "eleventy --serve --quiet --config 11ty.config.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.9.0",
    "@fullhuman/postcss-purgecss": "https://github.com/mattwaler/postcss-purgecss",
    "@starptech/prettyhtml": "^0.10.0",
    "autoprefixer": "^9.6.1",
    "axios": "^0.19.0",
    "concurrently": "^4.1.2",
    "dotenv": "^8.1.0",
    "eslint": "^6.2.2",
    "eslint-plugin-import": "^2.18.2",
    "eslint-plugin-node": "^10.0.0",
    "eslint-plugin-promise": "^4.2.1",
    "parcel-bundler": "^1.12.3",
    "tailwindcss": "^1.1.2",
    "watch": "^1.0.2"
  }
}
